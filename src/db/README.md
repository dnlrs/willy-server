## Visual Studio configuration (DB)


#### Schemas

`packets`(**hash**, *ssid*, rssi, **mac**, *channel*, *seq_number*, timestamp, **anchor_mac**);  
`devices`(**mac**, **timestamp**, pos_x, pos_y);

> Note
> - **bold** attributes compose the primary key  
> - *italic* attributes can be `NULL`
